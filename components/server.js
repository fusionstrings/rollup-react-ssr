import React from 'react';
// import ReactDOMServer from 'react-dom/server';
import App from './app.js';

// const ssrContent = props => ReactDOMServer.renderToString(React.createElement(App, props));

export default App;
